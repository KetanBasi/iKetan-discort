import re, discord
from discord import colour
import discord.ext
# import manual as mannn
from discord import message

# def embed(description, title=None, colour=discord.Colour.orange(), *fields):
#     _message = discord.Embed(
#         title = title,
#         description = description,
#         colour = colour
#     )
#     for _item in fields:
#         _message.add_field(name=_item['title'], value=_item['description'])
#     return _message

# def manual(command_prefix, manual_text):
#     return embed(manual_text, f'Command_manual: {command_prefix}say')
